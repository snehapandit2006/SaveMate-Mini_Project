
  # SaveMate Workflow Overview

  This is a code bundle for SaveMate Workflow Overview. The original project is available at https://www.figma.com/design/jdd0mFaTd36XGCvrv9k7qG/SaveMate-Workflow-Overview.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  